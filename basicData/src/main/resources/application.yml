server:
  port: 9081
spring:
  application:
    name: basicData  # 服务间调用所使用的名称
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/